<% content_for :title, "Editar Artículo - Base de Conocimiento" %>

<div class="page-header">
  <div>
    <h1 class="page-title">Editar Artículo</h1>
    <p class="page-subtitle"><%= @article.title %></p>
  </div>
  <div>
    <%= link_to "← Volver", kb_article_path(@article), class: "btn btn-secondary btn-sm" %>
  </div>
</div>

<%= render "form", article: @article %>
